/**
 * Render the header for the website with a welcome message.
 * @returns the header
 */
export default function Header() {
    return (
        <div> 
            <h1 className="text-2xl"> Welcome to Spark</h1>
        </div>
    );
}
